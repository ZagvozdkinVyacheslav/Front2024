import {Container} from "@mantine/core";
import {useState} from "react";

const Operators = () => {
    return (
        <div style={{
            width: '100%',
            height: '100%',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            position: 'relative',
            gap: 15,
        }}>
            <Container>
                ВОТ ТУТ БУДЕТ ТАБЛИЧКА С ОПЕРАТОРАМИ (ТОЛЬКО ДЛЯ АДМИНА)
            </Container>
        </div>
    );
};

export default Operators;